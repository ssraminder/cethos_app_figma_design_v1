# Preferences

How the user wants you to approach work in this project. Add any time the user corrects your approach OR confirms a non-obvious approach worked. Include the *why* so you can judge edge cases.

## Format
- **Rule** — short statement
  - **Why:** reason given (incident, principle, constraint)
  - **How to apply:** when/where this kicks in

## Code & implementation

- **Fail loud on missing template / lookup-table config; never silently fall back to a wrong locale or default** — when a feature picks a row by language / certification type / jurisdiction, missing rows must return a structured error (e.g. `code: "AFFIDAVIT_TEMPLATE_MISSING"`) with a remediation hint. Never substitute English content on a non-English target, or a default cert template on a different cert type.
  - **Why:** ISO 17100 reproducibility + customer-trust — a silent fallback ships a wrong-language certification that the customer cannot easily detect until it's been submitted to a government agency. The "loud failure surfaces in the admin step card as a red chip" pattern (per 2026-05-21 affidavit) gives staff a clear remediation path.
  - **How to apply:** Edge functions return 422 + `code`. Admin UI shows the error chip + an "Override manually" affordance. Don't add a `defaults_to_english=true` flag unless explicitly authorized.

- **Verify handover-doc schema claims against prod before writing the migration** — a handover doc lists "current production schema" snapshots that can be stale. Spot-check `information_schema.columns` + sample rows for every table the migration / function will touch before authoring SQL or edge-function code.
  - **Why:** The 2026-05-21 handover §10 said `orders.certification_type` was a human-readable string ("Oath Commissioner"); prod has `orders.certification_type_id UUID → certification_types(code/name)`. Three other §10 claims (step_deliveries.kind, step_deliveries.vendor_id, intended_uses.label) were also wrong. Building on the doc verbatim would have shipped broken queries.
  - **How to apply:** Before drafting any migration or edge function, run quick `mcp__supabase__execute_sql` probes (`SELECT column_name FROM information_schema.columns ...`) on every referenced table. Bring corrections back to the user with the proposed defaults before writing SQL.

- **AI-assisted features: deterministic core + Claude prose** — when an AI feature picks a value (rate, action, score), the value comes from a deterministic formula with documented inputs. Claude is allowed to write the human-readable reasoning paragraph, but never picks the number that bypasses the bounds.
  - **Why:** ISO 17100 auditability — every recommendation must be reproducible from logged inputs. Claude hallucination is unacceptable for billing-impacting decisions.
  - **How to apply:** Store inputs + multipliers + prompt_version in an audit table. Hard bounds enforced server-side regardless of model output. Fall back to template prose when Claude unavailable.

- **Anti-lowball floor on vendor-facing pricing** — when computing a vendor rate, never recommend below 12% of client rate regardless of test score / country COL / experience. "Don't insult with a lowball" is the user's explicit framing.
  - **Why:** Vendor retention. Mediocre test + low-COL country could math out to an offensive number; protect against it.
  - **How to apply:** Hard floor in the deterministic formula. Test by checking that a junior with bad test in a low-COL country still gets a defensible rate.

- **Aggressive counter-back tactic** — when negotiating against a vendor counter, anchor at ~30% of the way from their counter back toward our original (not midpoint). Always cite specific data: pool median, ceiling, vendor history, COL bucket.
  - **Why:** Negotiation theory — meeting in the middle leaves money on the table. 30% anchor signals firm pushback while still moving.
  - **How to apply:** `aggressive_counter_anchor_rate = original + (counter - original) * 0.30` baked into the prompt; deterministic fallback uses the same formula.

- **Edge functions deployed with --no-verify-jwt** — gateway JWT verification is off project-wide. Functions validate session tokens internally (vendor portal uses custom JWT; applicant flows are public; staff JWT arrives as header but isn't gateway-validated).
  - **Why:** Mixed auth contexts (vendor session ≠ Supabase auth, applicant has no auth) make gateway validation impractical.
  - **How to apply:** Always `supabase functions deploy <name> --no-verify-jwt`. Validate inside the function where the auth model demands it.

- **Pricing convention: subtotal = translation only, certification always separate** — since 2026-05-11. Every consumer computes `pre_tax = total − tax_amount` rather than summing components (correct under old + new schemas without backfill).
  - **Why:** Historical inconsistency between `recalculate_quote_totals` (subtotal=translation+cert) and `recalculate_quote_from_groups` (subtotal=translation only) caused certification to double-count on display.
  - **How to apply:** Don't add to the legacy sum-of-components pattern. New display code derives `pre_tax = total - tax`.

## Communication

- **Staircase delivery** — user prefers explicit phases. Ship Phase 1, then Phase 2. "Begin small, and grow."
- **Terse direction; execute on green light** — user gives short answers like "B deploy them in batch" or "1. do recommended" (answering numbered open questions). Don't re-plan after that — execute.
- **Pivot quickly mid-conversation** — user iterates on design. When they clarify intent (e.g. "target = deferred, not flat-amount"), implement the correction in the same session as a follow-up PR, not a long discussion.
- **PR-per-feature workflow** — each phase or fix lands as its own PR. Open + merge in the same session unless the user pauses.
- **Recommend first on big work, then execute; "decide for me" = use judgment** — for substantial builds the user wants your recommended approach up front ("how do you think we should proceed?") plus at most one or two real decisions, then expects you to *proceed* without repeated check-ins. When they say "decide for me, whatever is better," pick the better option and act. Don't keep re-confirming after a green light — repeated deferral/checkpointing frustrates them.
- **Spoon-feed staff-facing docs** — SOPs, guides, and validation scripts are for colleagues new to *both* the task and the system, so write them "like spoonfeeding a baby": every exact click, what to type, and what they should see — simple yet extensive — with **annotated real screenshots** (not mockups). The bar is "a brand-new person can replicate it unaided."
- **Interactive, assignable, signed-off training > download-only docs (keep both)** — staff training should live in the portal: assignable to a person, completion-tracked, signed-off, with an audit log of who completed it. Keep a controlled portable copy too (now HTML — see next), as the printable / audit-binder copy.

- **Portal guides & manuals = self-contained HTML, viewable in-portal — NOT Word/.docx** — build staff/vendor guides as one self-contained `.html` file (real portal screenshots embedded as base64, branded teal/slate + Cethos logo), store in the Documents & Manuals library (`/admin/documents`, `manage-portal-documents`), and read it inline in the portal. HTML is the STANDARD format for new guides going forward.
  - **Why:** user directive 2026-06-25 — "we don't need a word document… created in html format… this can be used as standard for everything." Web-native, viewable without downloading, easy to generate/version, portable single file. (Replaces the docx-build approach.)
  - **How to apply:** capture real screenshots with the Playwright harness in `e2e/` (auth by copying the live Chrome session's `localStorage['cethos-auth']`+`staffSession` into `e2e/.auth/admin.json`); crop + embed as base64 via a node build script (see `e2e/build-coa-guide-html.mjs`). Upload via `manage-portal-documents` `create` with the file part `type=text/html`. **Gotcha:** Supabase Storage serves HTML as `text/plain` + `nosniff` (anti-XSS), so a signed URL renders as raw source — the `view_url` action instead returns the HTML `content`, rendered in a sandboxed `<iframe srcdoc>` in `/admin/documents` (the "View" eye button).
- **Comprehensive handovers + live memory** — for long or multi-session work, write a standalone handover (current state, ordered next steps, all IDs, machinery, hard rules) and keep the project memory current so a fresh session continues seamlessly.

## Tooling & workflow

- **Two repos, one Supabase project** — admin at `D:\cethos\portal\cethos_app_figma_design_v1` (also accessed via temp worktree at `C:\Users\RaminderShah\AppData\Local\Temp\elastic-kapitsa` ↔ `/tmp/elastic-kapitsa`), vendor at `D:\cethos-vendor`. Both deploy edge functions to `lmzoyezvsjgsxveoakdr`.
- **Worktree path quirks** — Grep/Read may not see `/tmp/elastic-kapitsa` directly from Windows; use the `C:\Users\RaminderShah\AppData\Local\Temp\elastic-kapitsa` form for these tools. Bash commands can use `/tmp/` paths fine.
- **Cron via pg_cron + pg_net** — both extensions enabled. Schedule recurring tasks with `cron.schedule(...)` calling `net.http_post(...)` to an edge function URL. Idempotent: `cron.unschedule()` first if re-running.
- **Migrations: apply + commit** — `mcp__supabase__apply_migration` for prod, then write the same SQL to `supabase/migrations/<timestamp>_<name>.sql` for the repo. The migration applies first, the file lands second so the repo reflects what's already in prod.
- **PR review-and-merge in one shot** — user often says "merge" right after a PR opens. Auto mode encourages this. Don't wait for explicit review unless something risky landed.

## Client onboarding, QMS & audit-readiness

- **Back-enter existing client work via SQL, not admin-create-order** — when bringing a client's already-done or in-flight orders into the portal in bulk (e.g. Welocalize, RWS), build them via SQL (dry-run → prove one → bulk), NOT the admin create-order flow. Orders are **un-delivered shells** (status `in_production` / work `pending` / invoice `unbilled`) unless a deliverable already exists (then replicate it → QA → complete). PM is chosen **per-project**, not per-client.
  - **Why:** admin-create-order emails the customer; back-entry must be silent.
  - **How to apply:** use the SQL clone/build helpers; verify on the live portal afterwards.
- **Never write "cloned from" (or any copy-paste lineage) on a record** — order notes, step instructions, etc. must read as bespoke work.
  - **Why:** IQVIA-sensitive — lineage text implies copied, non-original deliverables.
- **Cethos is ISO 17100-ALIGNED, not certified** — never claim or imply certification anywhere, internal or outward (Stage 2 target ~Dec 2026). Use "conforms to / aligned with."
  - **Why:** a false certification claim is audit + legal exposure.
- **Validate a new SOP/process against the live system before calling it "done"** — write a dummy-data, step-by-step validation script; have an **independent colleague replicate every step and mark pass/fail + notes**; only sign off when all steps pass. Use clearly-marked dummy data (**ZZ-TEST** prefix) and clean it up (cancel the test order) afterwards.
  - **Why:** proves the written SOP matches the system; the signed pass/fail script is documented audit evidence (UAT/PQ).
  - **How to apply:** never qualify a test/trial vendor (qualification is irreversible); orders are cancellable, so dummy orders are safe to create and delete.

- **Sensitive-domain clients must not hit AI — per-client `customers.ai_processing_enabled` toggle (default ON, OFF for COA clients)** — Welocalize, RWS, TransPerfect set OFF (migration 20260625). Enforced **server-side** (`generate-order-instructions` refuses for AI-disabled customers, fail-loud, before any Anthropic call) AND the "AI-generated job instructions / Generate" panel is hidden on their orders (`OrderCommunicationsTab` gets `aiEnabled` from `AdminOrderDetail`). Toggle lives on the client profile (`CustomerDetail` → save via `admin-manage-customer` whitelist).
  - **Why:** IQVIA audit attestation that COA/sensitive-domain jobs don't go through AI. The AI brief sends the client communications + attachments to Claude, so it WAS hitting AI on COA orders.
  - **Integrity rule:** never doctor/crop screenshots to hide a live AI feature for an audit — disable the feature for real (per-client), then recapture clean screenshots. **PENDING:** the COA guide (TRN-COA-001 HTML, unpublished), its markdown source, **TRN-RWS-001 (published)**, and the Welocalize handover still reference/show the AI "Generate vendor brief" step and MUST be scrubbed + screenshots recaptured before publishing.

- **Complaints & nonconformities link to an order/project; the CAPA trail shows ON the order** — the Log-complaint / New-nonconformity modals (`client/components/admin/quality/`) have an "Linked order / project" picker that sets `order_id` (the `qms_create_complaint`/`qms_create_nonconformity` RPCs already persist it from the payload — backend was ready, only the UI lacked the picker). The order page shows a **"Quality — Complaints & CAPA"** trail (`OrderQualityTrail` → `quality-read:list_for_order` → new RPC `qms_list_for_order(order_id)`). Built for IQVIA traceability (ties to NC-2026-00004 "CAPA/complaints not being logged").
  - **Worked CAPA example (RWS):** the RWS M26-266 / **261**-L1962A-ABVLV / BSFS / Tamil-Singapore cognitive-debriefing complaint = order **ORD-2026-10365**, which is mis-recorded (client_project_number **251**-L1962A-ABVLV not 261; PO **M26-266 missing**; target language generic **"Tamil"** not Tamil-Singapore) — those defects ARE the CAPA corrective actions. Log complaint+NC+CAPA via the **admin UI (Chrome MCP), NOT DB backdoor**, screenshot each step → build an **interactive, assignable, auditable** staff training (CAPA & complaint handling) in the live `cvp_trainings` system. **all trainings must be auditable** (per-person completion + sign-off).
  - **DONE 2026-06-25/26 (all via Chrome MCP except the seed):** complaint **CMP-2026-00005** → NC **NC-2026-00007** (root cause 5-whys; **Kiran Sekhon** linked, **0 perf events** — deferred-attribution proven) → **CAPA-2026-00007/00008**, on ORD-2026-10365's "Quality — Complaints & CAPA" card. Design enhancements shipped: **#1127** deferred/attributed performance impact + link-linguist, **#1129** correspondence/updates log. **Training SHIPPED:** `cvp_trainings` slug **`capa-complaint-handling`** (audience=staff, **5 lessons + 5 quiz**, pass 80) — live + verified in the learner at `/admin/trainings/capa-complaint-handling/overview`. Seeded via SQL (no authoring UI — `AdminTrainings` is a completion tracker filtered to audience=linguist, so staff trainings need a staff index/assign path). **OPEN:** (a) **screenshots** — 6 annotated shots captured + staged in `e2e/output/capa-training/screenshots/cropped/`; attach via `cvp_training_lessons.screenshot_paths` after uploading to the **training-assets** bucket (policy `training_assets_authenticated_insert` added, but an out-of-band staff-token upload hits Storage RLS-403 — upload via the app/dashboard, then UPDATE screenshot_paths). Re-capture: `node e2e/capture-capa-training.mjs` (auth = copy live Chrome `cethos-auth`+`staffSession` → `e2e/.auth/admin.json`). (b) **assign + complete** — assign to staff via `cvp_training_assignments` (staff_user_id) so the "I've read this" per-lesson acknowledgement (`cvp_training_lesson_progress`) records the auditable completion. **The staff trainings UI ALREADY exists** (don't rebuild): nav **"Trainings"** → `/admin/trainings` (`TrainingsList`, shows ALL active trainings to admins, no audience filter) → `/admin/trainings/:slug` (`TrainingOverview`) → `/admin/trainings/:slug/assign` (`TrainingAssign`) → `/admin/trainings/:slug/:lessonSlug` (`TrainingLesson` player). The **separate** "Linguist Trainings" nav = `/admin/qms/training-records` (`AdminTrainings`) is just the completion tracker, filtered to audience=linguist — that's why staff trainings don't appear there.
  - **e2e-VERIFIED on prod 2026-06-26 (Chrome MCP):** assign → start → ack all 5 lessons ("I've read this") → `cvp_training_assignments.completed_at` auto-stamps when ackedCount≥totalLessons (`markLessonAcknowledged`, `client/lib/trainings.ts`). Confirmed completed record for raminder@cethos.com (5/5, "Completed" badge). **GAP:** `quiz_enabled=true` + 5 `cvp_training_quiz_questions` exist for CAPA but **NO quiz UI is rendered** (TrainingOverview/TrainingLesson ignore quiz) — completion = lesson-acks only; the quiz is currently dead data. A graded quiz gate needs building if wanted.
  - **Training classification (audience):** STAFF = CAPA & Complaint Handling, Vendor Management. VENDOR/linguist = COA Linguistic Validation, Confidentiality & Data Protection, GCP for Clinical Linguists, ISO 17100 Process & QA. Staff guides = TRN-COA-001, TRN-RWS-001. **Register `REG-TRN-001` (Documents & Manuals → QMS Core, published, in-portal viewable) is v1.1 — grouped into Staff vs Vendor sections.** Generator: `e2e/build-training-register.mjs` → `docs/guides/Cethos-Training-Register.html` → `manage-portal-documents` `add_version`. No per-doc deep URL; access via `/admin/documents`.
  - **TRAININGS REDESIGN (user 2026-06-26): one hub classified by audience + bulk multiselect assign + single-confirm completion + no quiz. Decisions: (1) vendor visibility = AUTO-match (cvp_linguist_trainings_for_vendor) + explicit assignments ON TOP (not replace); (2) roll out admin/staff first, then vendor (cross-repo).** **PHASE 1 SHIPPED + e2e-verified on prod (admin repo):** #1135 single-confirm completion (`confirmTrainingComplete` acks all lessons + stamps completed_at in one go; per-lesson "I've read this" REMOVED from TrainingLesson; checkbox+"Complete training" on TrainingOverview); #1137 one classified hub (`TrainingsList` groups Staff vs Vendor by audience; "Completion records" link → `/admin/qms/training-records`; removed duplicate "Linguist Trainings" nav from Quality in `AdminLayout`); #1138 bulk assign (`/admin/trainings/bulk-assign` `BulkAssignTrainings` = multiselect trainings × multiselect staff + select-all + due date → `createAssignmentsBulk`). **PHASE 2 = NOT STARTED (cross-repo, the headline vendor ask):** assign trainings to VENDORS using the **vendor-list filters** (`AdminVendorsList.tsx`: status/availability/vendor_type/language(target_languages)/country/portal/CV/NDA/qms qualification — queries `vendors` directly) + multiselect. Needs: a vendor assignment path — **`cvp_training_assignments` has staff_user_id ONLY (no vendor col)** → add nullable `vendor_id` (CHECK one-of staff/vendor) OR new table; surface assigned trainings in the **vendor portal** (`D:\cethos-vendor`: `vendor-get-trainings` edge fn + `cvp_linguist_trainings_for_vendor` RPC → include explicit assignments, mark "required"; completion already via `cvp_training_completions` method=online + `admin-record-training-completion` for offline). Vendor portal training UI already COMPLETE (list+detail+single "Mark as complete").
  - **CHANGE TRAINING TYPE (user 2026-06-26) — SHIPPED + e2e-verified (#1140):** admin can switch a training's audience Staff↔Vendor via a **Type** selector on `TrainingOverview` (admin-only, next to "Assign to staff"). `updateTrainingAudience(trainingId, audience)` updates `cvp_trainings.audience` **directly from the client** — works because `cvp_trainings` RLS = `ALL to authenticated USING cvp_is_training_admin()` (super_admin qualifies; no edge fn needed). e2e on prod: CAPA staff→vendor → DB audience=linguist + hub re-grouped under "Vendor trainings" + `cvp_linguist_trainings_for_vendor` returns it (count 1); reverted vendor→staff → count 0. **VENDOR SIDE NEEDS NO CODE CHANGE** (verified, not assumed): all 3 vendor edge fns (`vendor-get-trainings`, `vendor-get-training-detail`, `vendor-mark-training-complete`) authorize via `cvp_linguist_trainings_for_vendor` which filters `audience='linguist'` → type change propagates to the vendor portal automatically + securely (de-typed training → detail/complete return 403). Note: switching to Vendor leaves `applies_to` at its default `{"scope":"universal"}` → shows for ALL vendors (per-vendor targeting = the separate Phase 2 work).
  - **VENDOR TRAINING REWORK (user 2026-06-26): total rework of vendor training design + content — interactive, example-rich.** Root rendering bug: the vendor portal had NO markdown lib — `TrainingDetail.tsx` used a hand-rolled regex `renderMarkdown` that escaped HTML + turned numbered lists into bullets. **User decisions:** include visual step pipelines + expandable worked examples + key-rule callouts (all required) + **GRADED knowledge checks built as a NEW step** (re-activates the dormant `cvp_training_quiz_questions`/`pass_threshold`/`quiz_score`/`quiz_enabled` infra — distinct from staff trainings' single-confirm); roll out COA first as a live proof, then the other 3. **ARCH:** new `cvp_training_lessons.content_blocks jsonb` = ordered typed blocks `{type: prose|steps|example|callout|comparison}`, NULL falls back to `body_markdown`. Shared `LessonBlocks` renderer **mirrored in BOTH repos** (vendor `apps/vendor/src/components/trainings/LessonBlocks.tsx`; admin `client/pages/admin/trainings/LessonBlocks.tsx` — dependency-free lightweight md for prose, keeps numbered lists). **PHASE A1 SHIPPED + verified live (admin #1145, vendor cethosvendorportal#276, vendor-get-training-detail deployed w/ content_blocks):** COA LV all 4 lessons reworked into blocks (steps/examples/callouts/comparisons; real examples: reconciliation, CD probe, ALCOA+ change-log). Verified the interactive render + accordion/expand on prod via the ADMIN player (TrainingLesson now renders content_blocks too — gives an admin preview path since vendor.cethos.com needs a vendor login). **STEPPER (user feedback "should be in steps, not a scrollable lengthy document") SHIPPED (admin #1148, vendor cethosvendorportal#278):** vendor `TrainingDetail` rebuilt as a one-lesson-per-step **wizard** (segmented progress + Back/Next + Mark-complete on last step); admin `TrainingLesson` got a matching "Step X of N" + segmented progress header. **PHASE B SHIPPED (migration `20260626_phaseb_vendor_training_blocks`):** all 3 other vendor trainings reworked into blocks — Confidentiality & Data Protection (4 lessons: NDA allow/forbid comparison, AI-shortcut breach example, PII-or-not example, incident steps), GCP (3: 4-principles steps, ALCOA+ steps + query-vs-guess example, do/don't), ISO 17100 (4: TEP steps, reviser-checks steps, pre-delivery checklist, feedback steps). **So ALL 4 vendor trainings now use interactive blocks + the stepper.** **VENDOR ASSIGNMENT (Phase 2) SHIPPED + e2e-verified:** **2a (admin #1151):** `cvp_training_assignments` gained nullable `vendor_id` (migration `20260626_training_assignments_vendor`; CHECK exactly one of staff_user_id/vendor_id; unique partial index on training+vendor). Vendor-type training overview now shows **"Assign to vendors"** → `/admin/trainings/:slug/assign-vendors` (`VendorAssign` page = vendor-directory filters search/status/availability/vendor_type/language/country + **select-all N matching** + bulk `assignVendorsBulk`, idempotent). Direct client insert works (no edge fn). **2b (admin #1153 RPC migration `20260626_linguist_trainings_required_flag` + vendor cethosvendorportal#279):** `cvp_linguist_trainings_for_vendor` now UNIONs explicit assignments on top of auto-match + returns a **`required`** flag (assigned sort first); vendor edge fns pass it through UNCHANGED (no redeploy); vendor `TrainingsList` shows a teal **Required** badge. e2e on prod: assigned COA→vendor via the UI (select-all=1000 worked; assigned 1) → `cvp_training_assignments` row + RPC `required=true` → cleaned up the test row. **STILL PENDING: Phase A2 = the graded knowledge-check step** (build the quiz UI in the vendor flow + grade vs pass_threshold + record quiz_score + gate completion; infra dormant). **FIXED 2026-06-26 (the 1000-cap bit once active vendors crossed 1000 — prod had 1,577 active but the picker showed/assigned only the first 1000 alphabetically, silently dropping 577):** `listVendorsForAssign` now **pages with `.range()` in 1000-row windows** until drained (PostgREST `max-rows=1000` defeats `.limit()`), so the list + "select all" + the "N match" count cover EVERY match. Same `.range()` paging added to `assignVendorsBulk`'s existing-assignment dedup query (was capped → could re-insert dupes and hit the unique index on >1000-assignment trainings) + inserts now chunk at 500. **Language filter** was an exact jsonb-element match (`.contains([code])`) → silently missed region-coded vendors (`FR` matched neither `FR-CA` nor `FR-FR`); now matched **client-side by family** (`code===q || code.startsWith(q+'-')`) after the full fetch (prod proof: FR 28→84, PT 6→26, SV 16→39 active vendors). Files: `client/lib/trainings.ts`, `client/pages/admin/trainings/VendorAssign.tsx`. Content is AI-drafted → user must review for clinical/LV domain accuracy.
  - **VENDOR-ASSIGNMENT EMAIL SHIPPED 2026-06-26 (user wants vendors notified + reminded, triage to answer questions).** New edge fn **`vendor-send-training-assignment`** (Mailgun — user chose Mailgun; both Brevo+Mailgun exist, `_shared/mailgun.ts` is canonical; deployed `--no-verify-jwt`; **self-contained single file** — inlines a trimmed brand shell so deployed==committed, avoids `_shared` bundling via MCP deploy). Reply-To **`vm@cethos.com`** → vendor questions route into the VM inbound/triage inbox automatically. **Mailgun BATCH send via `recipient-variables`** (1000/call) → 1,577-vendor rollout = 2 API calls, NOT a per-record loop (respects CLAUDE.md bulk-email rule). Modes: `test_email` (no DB writes) / `dry_run` / real (`vendor_ids`+`due_at`). Per-recipient **`notification_log`** rows (event_type `vendor_training_assignment`) = audit + future reminder dedup. Wired into VendorAssign: `assignVendorsBulk` now returns `{count, vendorIds}` (newly-inserted only) + **"Notify vendors by email" checkbox (default ON)** → `notifyVendorsOfAssignment` (invokes the fn, emails only the just-assigned). **TESTED on prod → ss.raminder@gmail.com** (Mailgun accepted, domain vendors.cethos.com, [TEST] subject). **ROLLOUT ORDER (user): GENERAL vendor trainings FIRST** = Confidentiality & Data Protection + Secure File Handling + ISO 17100 Process & QA → all 1,577 active; THEN subject-wise (Forward/Back/Reconciliation/Cog-Debriefing/COA; GCP=clinical). Visibility logic `cvp_linguist_trainings_for_vendor`: universal→all + subject_matter-match + explicit assignment(`required=true`); the 2 universal ones already auto-show (assigning adds Required+due+audit+email). **PENDING: (1) periodic-reminder cron for incomplete required trainings (notification_log dedup ready); (2) train the VM triage to answer vendor training questions.** **Vendor-portal verified (cross-repo `D:\cethos-vendor`, `apps/vendor`):** trainings DO surface — routes `/trainings` (list `TrainingsList`) + `/trainings/:training_id` (detail), fed by `vendor-get-trainings`→`cvp_linguist_trainings_for_vendor`; universal show to all, assigned show once assigned; the teal **Required** badge + top-sort render only when `required=true` (i.e. an explicit `cvp_training_assignments` row). Email CTA **deep-links to `/trainings/:training_id`** ("Start the training") — confirmed deployed==source (fn v2). **🔴 BUG FOUND+FIXED via impersonation test 2026-06-26:** the vendor portal Trainings page was **404ing for ALL vendors** ("Unexpected token '<'… not valid JSON") — `vendorTrainings.ts` routed through the `/sb/*` Netlify-lambda proxy, but the training endpoints have no lambda/redirect there (the `/sb` proxy only covers auth/jobs/profile/CV…). Fixed in **vendor PR #280** (`cethosvendorportal`): call `${FUNCTIONS_BASE}/vendor-get-trainings` directly like every peer module (guides/roster/quiz). **Verified live via admin "View as vendor" impersonation:** list renders 3 trainings (assigned one shows **Required** + top-sorted) and the detail/stepper at `/trainings/:id` (the email's deep-link target) loads. **TESTING TIP: admin VendorDetailHeader → Actions → "View as vendor" mints a 30-min vendor session (`admin-impersonate-vendor`) and opens vendor.cethos.com — the way to e2e-test vendor-portal changes without a vendor login.** Test vendor: "Test Vendor (Dutch→English)" `ss.raminder+dutchtest@gmail.com` id `78dc3e2d-b6c2-49ec-bec2-b2a9eba480c7`. **Completion flow e2e-verified 2026-06-26:** vendor "Mark as complete" → RPC `cvp_record_training_completion` (upsert; UNIQUE vendor+training) writes `cvp_training_completions` (status=completed, method=online) → surfaces in admin **Linguist Trainings** log (`/admin/qms/training-records`=`AdminTrainings`: name/email/training/method/date + per-training counts) AND vendor portal "Completed" badge. Offline path = `admin-record-training-completion` ("Record offline completion" modal) or the RPC w/ `method='offline'`. **Portal assign→email verified e2e via Chrome** (assigned Back Translation to vendor "Raminder" ss.raminder@gmail.com w/ Notify on → assignment row + `notification_log` status=sent + real Mailgun email). **Fixed `AdminTrainings` 200-row cap** (counts now from real COUNT queries, was derived from a `.limit(200)` slice → under-reported after bulk loads; recent feed→500). **Offline-backfill artifacts in user's Downloads:** `cethos-active-vendors-2026-06-26.csv` (1,600 active), `cethos-offline-training-backfill.sql` (by-EMAIL upsert template + all 9 training ids), `cethos-vendor-training-completions.xlsx` (team enters completion DATE per training column → load as method=offline). **STILL PENDING: the rollout — assign the 3 GENERAL trainings (Confidentiality & Data Protection + ISO 17100 Process & QA + Secure File Handling) to all ~1,600 active w/ email (note: 3 emails/vendor unless a combined-digest is built); reminder cron; VM triage training.**
  - **"NEW TRAINING" AUTHORING UI + 11 NEW QMS TRAININGS (user 2026-06-26).** Self-serve create shipped (#1158): hub **"New training"** button → `TrainingEditor` (`/admin/trainings/new`) — title / type (Staff|Vendor) / category / description + add/reorder lessons + per-lesson content_blocks via template buttons (prose/steps/example/callout/comparison) + live preview; saves direct to cvp_trainings + cvp_training_lessons via `createTraining`/`saveLessons` (RLS `cvp_is_training_admin()` allows direct client write — NO edge fn). v1 is a blocks-as-JSON editor + preview; a full WYSIWYG block editor is a future option. **THEN seeded 11 NEW trainings** from the user's drafts (`~/Downloads/cethos_lv_training_drafts.md`) via **5 parallel general-purpose author-agents** → migrations `20260627_seed_training_*` (PR #1166). **Skipped 3 already-covered (user: "don't replace existing"): TG-LV-001≈COA LV, TG-IS-001≈Confidentiality, TG-QM-002≈CAPA.** Created **6 staff (universal)** = Project Management for LV Projects, Client Communication Standards, ISO 17100 Awareness, **Internal Auditor**, ISO 27001 Security Awareness, Incident Response Drill; **5 vendor (audience=linguist, applies_to scope=`assigned` → assignment-driven via the vendor filters, NOT universal/auto-shown to all)** = Forward Translation, Reconciliation, Back Translation, Cognitive Debriefing, Secure File Handling. Knowledge checks = in-lesson `example` blocks + **21 seeded `cvp_training_quiz_questions`** (quiz_enabled=false, dormant until Phase A2). DB-verified: 11 present, 155 content blocks all well-formed (0 malformed), correct audiences/scopes. **GOTCHAS: `cvp_training_lessons.body_markdown` is NOT NULL** (must supply even with content_blocks); **parallel `apply_migration` collides on the same-wall-clock-second schema_migrations version** → agents fell back to `execute_sql`. **Total trainings now = 17 (6 original + 11 new).** Content AI-drafted → user reviews for domain accuracy.

- **Quality complaints/NCs: link the linguist for traceability, but DEFER any performance-scorecard impact until the complaint/NC is RESOLVED — and only ding the linguist if the resolution attributes fault to them.** Current bug: `qms_create_complaint`/`qms_create_nonconformity` emit the perf event (`client_complaint` / `quality_issue` via `qms_emit_perf_event`) at CREATION on any vendor link, penalizing the linguist *during investigation* even when the root cause is Cethos-side (e.g. the RWS BSFS Tamil-Singapore case = intake/recordkeeping, not Kiran's interview work). Redesign: store the vendor link on create WITHOUT emitting; emit only at closure when staff mark it attributable to the linguist (needs an "attributed to linguist?" input on the close flow + an edit/link-vendor path for existing records).
  - **Why:** fair, accurate ISO performance monitoring — never penalize on suspicion. User directive 2026-06-25: "the system should be designed… and doesn't ding a profile until the complaint is resolved."

- **Complaint handling = lightweight tracker + correspondence log, NOT a full ticket/helpdesk.** Add a dated updates/correspondence log to complaints/NCs (status change · internal note · "reply sent to client" · "client responded"); reuse the linked order's **Client Communications** for the actual client emails (single source of truth). **Why:** ISO §4.6 wants evidence the resolution was communicated, without a helpdesk that duplicates order Messages/Client Communications. User directive 2026-06-25 (chose this over a full ticket system).

## Things to avoid

- **Bulk deploys without explicit authorization** — sandbox blocks mass deploys of many functions at once. Either deploy one at a time, or get a "deploy them in batch" green light. (Resolved per-session, but worth remembering when scope creeps.)
- **Force-push to remote branches** — blocked by sandbox. Don't rely on it for cleanup. If a PR branch picked up unrelated files, create a fresh branch off main and re-PR.
- **Hand-rolled `fetch` to Supabase functions from the admin UI** — omits Authorization + apikey headers. Use `supabase.functions.invoke` instead. Pitfall already burned us in `RecruitmentDetail.callEdgeFunction`.
- **Don't relitigate anonymization of customer names to vendors** — user decided 2026-05-05 it's not a goal. Only the PRJ project number is the anonymization layer.
- **Don't assume a function exists just because source is in the repo** — multiple functions had source committed but were never deployed. Test the endpoint or `mcp__supabase__list_edge_functions`.
